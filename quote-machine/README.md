# Quote Machine

Home Assistant add-on wrapper for `you209/Quote-Machine`.

> If the source repo is private, Home Assistant will fail to build this add-on. Make `you209/Quote-Machine` public or copy the app source directly into this folder and update the Dockerfile to use `COPY . .` instead of `git clone`.
