# Find My Local Pollie

Home Assistant add-on wrapper for `you209/Find-My-Local-Pollie`.

> If the source repo is private, Home Assistant will fail to build this add-on. Make `you209/Find-My-Local-Pollie` public or copy the app source directly into this folder and update the Dockerfile to use `COPY . .` instead of `git clone`.
