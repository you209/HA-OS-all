# ST Security Tool

Home Assistant add-on wrapper for `you209/ST`.

> If the source repo is private, Home Assistant will fail to build this add-on. Make `you209/ST` public or copy the app source directly into this folder and update the Dockerfile to use `COPY . .` instead of `git clone`.
