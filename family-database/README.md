# Family Database

Home Assistant add-on wrapper for `you209/Family-Database`.

This repo is public, so this add-on should be able to clone/build as long as the app Docker build dependencies are correct.
